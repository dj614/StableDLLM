# LLaDA task-pack configs

This directory will hold **overlay configs** for LLaDA tasks.

The framework defaults live under `src/configs/`. LLaDA configs should only
override task-specific fields (dataset paths, prompt/tokenizer options, etc.).
