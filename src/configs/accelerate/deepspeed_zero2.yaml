compute_environment: LOCAL_MACHINE
distributed_type: DEEPSPEED
num_machines: 1
num_processes: 2

deepspeed_config:
  # Use the repo-local DeepSpeed config (avoids depending on vendored upstream paths).
  deepspeed_config_file: src/configs/deepspeed/zero2_cpu_offload.json
  zero3_init_flag: false